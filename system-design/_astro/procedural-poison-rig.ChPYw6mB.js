import{D as P,p as E,r as W,a as q}from"./agent-memory.BAwcRrF9.js";import{readout as S,onThemeChange as D,palette as O,fitCanvas as H,label as k,stackLabels as Y}from"./rig.CyiChFZe.js";import"./context-rot.XoIS-gv1.js";import"./jit-context.CVgcykTl.js";import"./inference-gateway.B5Ydqc25.js";import"./kernel.DrC0sNW-.js";import"./limiters.CHKI6pwP.js";import"./bulkhead.DGoNKOeA.js";import"./inference.zvSQdG3n.js";import"./accelerators.BdwR2ykV.js";import"./rag-pipeline.CkifGwXq.js";import"./compaction.Cllc0NIp.js";const L=680,R=300,B=46,_=146,d=26,p=R-46,A=y=>`${(y*100).toFixed(1)}%`,b=y=>`${y>=0?"+":"−"}${Math.abs(y).toFixed(2)}`;class G extends HTMLElement{connectedCallback(){this.churnTenths=Math.round(P.churnPerSession*1e3),this.qTenths=Math.round(P.poisonRatio*10),this.ttl=30,this.innerHTML=`
      <div class="panel">
        <canvas role="img" aria-label="Expected value of a procedural rule against its age, crossing zero into net harm, with a revalidation TTL drawn as a sawtooth."></canvas>
        <div class="readouts"></div>
        <p class="verdict"></p>
        <div class="controls">
          <label>
            the world changes, per session
            <input type="range" min="2" max="50" step="2" value="${Math.round(P.churnPerSession*1e3)}" data-s="churn" aria-label="Chance per session that a recorded rule stops being true">
            <output class="num" data-o="churn"></output>
          </label>
          <label>
            a stale rule costs, in right ones
            <input type="range" min="0" max="50" step="5" value="${Math.round(P.poisonRatio*10)}" data-s="q" aria-label="What following one stale rule costs, as a multiple of what a fresh one gains">
            <output class="num" data-o="q"></output>
          </label>
          <label>
            re-verify every
            <input type="range" min="1" max="120" step="1" value="30" data-s="ttl" aria-label="Sessions between revalidations of the rule">
            <output class="num" data-o="ttl"></output>
          </label>
        </div>
      </div>`,this.canvas=this.querySelector("canvas");const i=this.querySelector(".readouts");this.ro={age:S(i,"net harm starts at"),fresh:S(i,"still true at the crossing"),mean:S(i,"mean value under this TTL"),tail:S(i,"harmful sessions per cycle"),share:S(i,"of every cycle spent harmful")},this.verdict=this.querySelector(".verdict");for(const[e,T]of[["churn","churnTenths"],["q","qTenths"],["ttl","ttl"]])this.querySelector(`[data-s="${e}"]`).addEventListener("input",h=>{this[T]=Number(h.target.value),this.draw()});D(()=>this.draw()),this.obs=new ResizeObserver(()=>this.draw()),this.obs.observe(this),this.draw()}disconnectedCallback(){this.obs?.disconnect()}draw(){const i=O(this),e=H(this.canvas,L,R);e.clearRect(0,0,L,R);const T=this.churnTenths/1e3,h=this.qTenths/10,a=this.ttl,v={churnPerSession:T,poisonRatio:h},s=E(v),r=W({ttlSessions:a,...v}),C=Math.max(Number.isFinite(s)?s*2.2:0,a*2.2,20),n=[20,40,60,90,120,180,240,320].find(t=>t>=C)??320,F=1,m=h===0?-.12:-Math.max(.4,h),c=B,x=L-_,l=t=>c+t/n*(x-c),f=t=>d+(F-t)/(F-m)*(p-d),w=f(0);h>0&&(e.fillStyle=i.crimson,e.globalAlpha=.08,e.fillRect(c,w,x-c,p-w),e.globalAlpha=1),e.strokeStyle=i.rule,e.lineWidth=1;const M=F-m>3.2?1:.5;for(let t=Math.ceil(m/M)*M;t<=F+1e-9;t+=M){const o=f(t);e.globalAlpha=Math.abs(t)<1e-9?.95:.26,e.beginPath(),e.moveTo(c,o),e.lineTo(x,o),e.stroke(),e.globalAlpha=1,k(e,t===0?"0":M>=1?t.toFixed(0):t.toFixed(1),c-6,o,i.slate,{size:8,align:"right"})}const N=n<=40?10:n<=120?20:40;for(let t=0;t<=n;t+=N)k(e,String(t),l(t),p+12,i.slate,{size:8,align:"center"});k(e,"sessions elapsed · each check resets the age to zero",c+(x-c)/2,p+26,i["ink-soft"],{size:8,align:"center"}),k(e,"value of following the rule, in fresh uses",c,d-12,i["ink-soft"],{size:8,align:"left"}),e.strokeStyle=i.slate,e.lineWidth=1.6,e.beginPath();for(let t=0;t<=n;t+=.5){const o=q({ageSessions:t,...v}).value,g=f(Math.max(m,o));t===0?e.moveTo(l(t),g):e.lineTo(l(t),g)}e.stroke(),e.lineWidth=2;for(let t=0;t<n;t+=a){e.strokeStyle=i.teal,e.beginPath();for(let o=0;o<=a&&t+o<=n;o+=.5){const g=q({ageSessions:o,...v}).value,V=f(Math.max(m,g));o===0?e.moveTo(l(t+o),V):e.lineTo(l(t+o),V)}if(e.stroke(),Number.isFinite(s)&&a>s){e.fillStyle=i.crimson,e.globalAlpha=.3,e.beginPath(),e.moveTo(l(Math.min(n,t+s)),w);for(let o=s;o<=a&&t+o<=n;o+=.5){const g=q({ageSessions:o,...v}).value;e.lineTo(l(t+o),f(Math.max(m,g)))}e.lineTo(l(Math.min(n,t+a)),w),e.closePath(),e.fill(),e.globalAlpha=1}}e.lineWidth=1,e.save(),e.strokeStyle=i.teal,e.setLineDash([1,3]);for(let t=a;t<n;t+=a)e.beginPath(),e.moveTo(l(t),d),e.lineTo(l(t),w),e.stroke();if(e.restore(),Number.isFinite(s)){e.save(),e.strokeStyle=i.crimson,e.setLineDash([3,3]);for(let t=0;t<n;t+=a){const o=t+s;o>n||s>a||(e.beginPath(),e.moveTo(l(o),d),e.lineTo(l(o),p),e.stroke())}s<=n&&s>a&&(e.beginPath(),e.moveTo(l(s),d),e.lineTo(l(s),p),e.stroke()),e.restore(),s<=n&&k(e,`net harm from ${s.toFixed(0)}`,l(s)+4,d+8,i.crimson,{size:8,align:"left"})}const z=q({ageSessions:n,...v}).value;Y(e,[{y:f(Math.max(m,z)),text:`unverified — ${b(z)} by ${n}`,colour:i.slate},{y:f(r.meanValue),text:`every ${a} — mean ${b(r.meanValue)}`,colour:i.teal}],{x:x+8,align:"left",minGap:11,minY:d,maxY:p});const u=!Number.isFinite(s);this.ro.age.set(u?"never":`${s.toFixed(1)} sessions`,u?"ok":s<20?"bad":"warn"),this.ro.fresh.set(u?"—":A(h/(1+h)),u?"ok":"warn"),this.ro.mean.set(b(r.meanValue),r.meanValue<=0?"bad":r.meanValue<.5?"warn":"ok"),this.ro.tail.set(u?"none":`${r.harmfulTailSessions} of ${a}`,r.harmfulTailSessions===0?"ok":"bad"),this.ro.share.set(u?"0%":A(r.harmfulTailSessions/a),r.harmfulTailSessions===0?"ok":r.harmfulTailSessions/a>.3?"bad":"warn");let $;h===0?$="With a stale rule costing nothing to follow, the value never crosses zero — it only decays towards it, which is the SEMANTIC tier's failure mode: a fact you no longer have. That is the whole difference between the two tiers, and it is this one slider. Raise it off zero and a floor appears under the axis.":u?$="Nothing churns, so nothing goes stale and no rule ever needs re-verifying. Raise the churn slider and a crossing appears.":a<=s?$=`The rule is worth ${b(1)} the day it is written and crosses into net harm at ${s.toFixed(1)} sessions — the point where it is only ${A(h/(1+h))} likely to still be true, which is a long way from "obviously wrong". Re-verifying every ${a} sessions keeps every cycle inside the positive half and holds the mean at ${b(r.meanValue)}. The interval that matters is the crossing, not the churn rate: this TTL is safe because it is shorter than ${s.toFixed(0)}, and nothing about the rule's content tells you that number.`:$=`The crossing is at ${s.toFixed(1)} sessions and the TTL is ${a}, so ${r.harmfulTailSessions} sessions of every cycle — ${A(r.harmfulTailSessions/a)} of the rule's life — are spent in the shaded region actively doing damage. The mean is ${b(r.meanValue)}${r.meanValue>0?", still positive, which is exactly how this survives review: the average looks fine and the tail is where the incidents are":", negative — the rule is worse than not having it at all"}. Nothing at read time can see any of this. The record is a correct record of a world that moved, so there is no check to add — only a TTL to shorten, to under ${Math.floor(s)}.`,this.verdict.textContent=$,this.canvas.setAttribute("aria-label",`A procedural rule starts worth one fresh use and, at ${(T*100).toFixed(1)} percent churn per session with a stale rule costing ${h.toFixed(1)} right ones, crosses into net harm at ${u?"never":`${s.toFixed(1)} sessions`}. Re-verified every ${a} sessions its mean value is ${r.meanValue.toFixed(2)}.`),this.querySelector('[data-o="churn"]').textContent=`${(T*100).toFixed(1)}%`,this.querySelector('[data-o="q"]').textContent=h.toFixed(1),this.querySelector('[data-o="ttl"]').textContent=`${a} session${a===1?"":"s"}`}}customElements.get("procedural-poison-rig")||customElements.define("procedural-poison-rig",G);
