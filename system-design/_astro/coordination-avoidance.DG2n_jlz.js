import{G as s}from"./crdts-and-operational-transform.CRdsZZrT.js";const r={"counter-increment":{invariantConfluent:!0,invariant:'none beyond "total = sum of increments"',reason:"a G-Counter merge (elementwise max) is commutative/associative/idempotent — no concurrent interleaving can produce a wrong total"},"set-add":{invariantConfluent:!0,invariant:"none — every add is unconditionally retained",reason:"an OR-Set add is a union of tagged, unique elements — concurrent adds never collide and never need to be reconciled against each other"},"account-withdrawal-with-nonnegative-invariant":{invariantConfluent:!1,invariant:"balance >= 0",reason:"two concurrent withdrawals can each read a stale balance, both see sufficient funds, both proceed, and the merged result goes negative — the lost-update anomaly"},"inventory-decrement-with-floor":{invariantConfluent:!1,invariant:"stock >= 0",reason:"structurally identical to the withdrawal case: two concurrent decrements can each independently pass the same floor check before either is visible to the other"}},f=Object.keys(r);function h(n){const e=r[n];if(!e)throw new RangeError(`unknown operation type: ${n}`);return e.invariantConfluent}function v(n){const e=r[n];if(!e)throw new RangeError(`unknown operation type: ${n}`);return{operationType:n,...e}}const l=.001;function w(n=3,e=1){const o=Array.from({length:n},()=>new s);let t=0;o.forEach((a,u)=>{for(let c=0;c<e;c++)a.increment(`replica-${u}`,1),t++});const i=new s;for(const a of o)i.merge(a),t++;return i.value(),t*l}function m(n,e){if(e<=0)throw new RangeError("uncoordinatedLatencyMs must be positive");if(n<0)throw new RangeError("coordinatedLatencyMs must be non-negative");return n/e}export{f as O,v as a,m as c,h as i,w as u};
