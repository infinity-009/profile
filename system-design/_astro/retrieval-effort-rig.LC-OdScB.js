import{M as N,h as W,C as h,b as Z,c as ee,a as te}from"./agentic-retrieval.BDEVTZJe.js";import{e as _}from"./context-rot.XoIS-gv1.js";import{readout as y,onThemeChange as se,palette as ne,fitCanvas as ae,label as b}from"./rig.CyiChFZe.js";import{P as R}from"./ttft-perception.0rancOKD.js";import"./rag-pipeline.CkifGwXq.js";import"./inference-gateway.B5Ydqc25.js";import"./kernel.DrC0sNW-.js";import"./limiters.CHKI6pwP.js";import"./bulkhead.DGoNKOeA.js";import"./inference.zvSQdG3n.js";import"./accelerators.BdwR2ykV.js";const A=680,F=340,oe=54,re=156,T=34,v=F-62,x=12,E=u=>`${(u*100).toFixed(1)}%`,o=u=>u>=.01?`${(u*100).toFixed(1)}%`:`${(u*100).toFixed(2)}%`,B=u=>`$${u.toFixed(3)}`;class ie extends HTMLElement{connectedCallback(){this.hops=3,this.methodIdx=2,this.innerHTML=`
      <div class="panel">
        <canvas role="img" aria-label="Retrieval recall and answered-question rate against the number of turns spent searching, with the answered curve peaking at an interior turn count and Nielsen's ten-second line marked."></canvas>
        <div class="readouts"></div>
        <p class="verdict"></p>
        <div class="controls">
          <label>
            hops the question needs
            <input type="range" min="1" max="5" step="1" value="3" data-s="hops"
                   aria-label="How many chunks the answer is spread across">
            <output class="num" data-o="hops"></output>
          </label>
          <label>
            retrieval
            <select data-s="method" aria-label="Which retrieval method each query uses">
              ${N.map((e,a)=>`<option value="${a}"${a===2?" selected":""}>${e.label}</option>`).join("")}
            </select>
          </label>
        </div>
      </div>`,this.canvas=this.querySelector("canvas");const s=this.querySelector(".readouts");this.ro={peak:y(s,"turns worth spending, and what it answers"),end:y(s,"at twelve turns instead"),one:y(s,"one query, one shot"),chunks:y(s,"chunks in the window at the peak"),cost:y(s,"at the peak: money, and wall-clock")},this.verdict=this.querySelector(".verdict"),this.querySelector('[data-s="hops"]').addEventListener("input",e=>{this.hops=Number(e.target.value),this.draw()}),this.querySelector('[data-s="method"]').addEventListener("change",e=>{this.methodIdx=Number(e.target.value),this.draw()}),se(()=>this.draw()),this.obs=new ResizeObserver(()=>this.draw()),this.obs.observe(this),this.draw()}disconnectedCallback(){this.obs?.disconnect()}rows(s,e){const a=[];for(let r=1;r<=x;r++){const l=Math.floor(r/s),i=l<1?0:W({hops:s,methodId:e,attempts:l}).agentic,$=r*h.chunksPerRetrieval,c=_({k:$,chunkTokens:h.chunkTokens,preTokens:h.baseTokens}).found;a.push({turns:r,attempts:l,recall:i,chunks:$,attention:c,answered:i*c})}return a}draw(){const s=ne(this),e=ae(this.canvas,A,F);e.clearRect(0,0,A,F);const a=this.hops,r=N[this.methodIdx],l=this.rows(a,r.id),i=W({hops:a,methodId:r.id,attempts:1}).oneShot,$=_({k:h.chunksPerRetrieval,chunkTokens:h.chunkTokens,preTokens:h.baseTokens}).found,c=i*$,p=oe,f=A-re,m=t=>p+(t-1)/(x-1)*(f-p),q=Math.max(.05,...l.map(t=>t.recall))*1.08,w=t=>v-Math.max(0,t)/q*(v-T);e.strokeStyle=s.rule;const L=q>.6?.2:q>.25?.1:.05;for(let t=L;t<=q;t+=L)e.beginPath(),e.moveTo(p,w(t)),e.lineTo(f,w(t)),e.stroke(),b(e,E(t),p-12,w(t)+3,s.slate,{size:8,align:"right"});const U=h.searchMs+h.hopModelMs,g=R.attentionLost/U;g>=1&&g<=x&&(e.save(),e.strokeStyle=s.crimson,e.setLineDash([4,3]),e.beginPath(),e.moveTo(m(g),T),e.lineTo(m(g),v),e.stroke(),e.restore(),b(e,"ten seconds",m(g)+4,T+8,s.crimson,{size:8,align:"left"}));const O=(t,k,M)=>{e.save(),e.strokeStyle=k,e.lineWidth=M,e.beginPath(),l.forEach((P,Y)=>{const D=m(P.turns),H=w(P[t]);Y===0?e.moveTo(D,H):e.lineTo(D,H)}),e.stroke(),e.restore()};O("recall",s.teal,2),O("answered",s.amber,2.4);const C=(t,k,M,P=4)=>{e.fillStyle=M,e.beginPath(),e.arc(t,k,P,0,Math.PI*2),e.fill()};C(m(1),w(i),s.slate,4),C(m(1),w(i*$),s.crimson,4);const n=[...l].sort((t,k)=>k.answered-t.answered)[0],X=Math.abs(n.answered-c)<1e-9,j=n.answered>c;e.strokeStyle=s.amber,e.lineWidth=2,e.beginPath(),e.arc(m(n.turns),w(n.answered),7,0,Math.PI*2),e.stroke(),e.lineWidth=1,e.strokeStyle=s.rule,e.beginPath(),e.moveTo(p,v),e.lineTo(f,v),e.stroke();for(let t=1;t<=x;t+=1)(t%2===1||t===x)&&b(e,String(t),m(t),v+18,s.slate,{size:8,align:"center"});b(e,"turns spent searching",p+(f-p)/2,v+34,s["ink-soft"],{size:8,align:"center"}),b(e,`a ${a}-hop question · ${r.label}`,p,T-18,s["ink-soft"],{size:8,align:"left"});const G=f+10;[{text:"every chunk retrieved",colour:s.teal},{text:"and still answered",colour:s.amber},null,{text:"one query: retrieved",colour:s.slate},{text:"one query: answered",colour:s.crimson}].forEach((t,k)=>{t&&b(e,t.text,G,T+8+k*13,t.colour,{size:8,align:"left"})});const d=l[l.length-1],z=Z({hops:a,attempts:Math.max(1,n.attempts)}),S=ee({hops:a,attempts:Math.max(1,n.attempts)});this.ro.peak.set(`${n.turns} turn${n.turns===1?"":"s"} → ${o(n.answered)}`,"ok"),this.ro.end.set(`${o(d.answered)} — ${d.answered<n.answered?`${((1-d.answered/n.answered)*100).toFixed(0)}% worse`:"still the best"}`,d.answered<n.answered?"bad":"ok"),this.ro.one.set(`${o(i)} retrieved → ${o(c)} answered`,c>=n.answered-1e-9?"ok":"bad"),this.ro.chunks.set(`${n.chunks}, against a best of ~15`,n.chunks>40?"bad":"warn"),this.ro.cost.set(`${B(z.usd)} · ${S.seconds.toFixed(1)}s`,S.ms>R.attentionLost?"bad":"warn");const I=te({methodId:r.id,attempts:1}).recall,J=a===1?n.turns===1?`At one hop there is nothing to chain, and this retriever is already at ${E(I)} on its first query — so the teal curve has almost nothing left to climb and every extra turn only adds chunks. The peak is turn one. `:`At one hop there is nothing to chain, but this retriever only reaches ${E(I)} on its first query, so a second one buys enough recall to pay for its own ${h.chunksPerRetrieval} chunks. The peak is turn ${n.turns}, and the turns after it are all loss. `:`A ${a}-hop chain cannot be completed in fewer than ${a} turns, so the teal curve is zero until turn ${a} and then climbs as each hop earns a second attempt. `,K=`The amber curve is the same recall times the chance the answer is still findable among ${h.chunksPerRetrieval} chunks per turn, and it peaks at ${n.turns} turns and ${o(n.answered)}. By twelve turns retrieval has reached ${o(d.recall)} and answers have fallen to ${o(d.answered)} — ${d.recall>n.recall?"more retrieved":"no more retrieved"}, ${((1-d.answered/n.answered)*100).toFixed(0)}% fewer answered. `,Q=X?`That peak is one query — the same ${h.chunksPerRetrieval} chunks, ${o(i)} retrieved, ${o(c)} answered. The loop cannot beat it here; it can only spend turns walking down the curve. `:j?`Against one query on the same question — ${o(i)} retrieved, ${o(c)} answered — the chain is worth ${(n.answered/c).toFixed(1)}×. `:`And one query on the same question answers ${o(c)}, which is BETTER than anything the chain reaches. On a question this shallow the loop is a cost with no upside. `,V=`At the peak that is ${B(z.usd)} and ${S.seconds.toFixed(1)} seconds of serial waiting${S.ms>R.attentionLost?" — past the ten-second line, so a user watching a spinner has already concluded it broke":", inside the ten-second line"}. The money is not the constraint; the two things that are, are the window and the clock.`;this.verdict.textContent=J+K+Q+V,this.canvas.setAttribute("aria-label",`Retrieval recall and answered rate against turns spent, for a ${a}-hop question with ${r.label}. Answers peak at ${n.turns} turns and ${(n.answered*100).toFixed(1)} percent, then fall to ${(d.answered*100).toFixed(1)} percent by twelve turns even though retrieval keeps rising. One query on the same question retrieves ${(i*100).toPrecision(3)} percent and answers ${(i*$*100).toPrecision(3)} percent.`),this.querySelector('[data-o="hops"]').textContent=a===1?"1 chunk":`${a} chunks, chained`}}customElements.get("retrieval-effort-rig")||customElements.define("retrieval-effort-rig",ie);
