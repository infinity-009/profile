import{m as oe,a as ae,w as re,b as h,c as ne,e as le,d as he,f as ce,g as K,P as de}from"./egress.v4gQgvb0.js";import{readout as S,onThemeChange as pe,palette as ue,fitCanvas as me,label as C,stackLabels as be,spreadLabels as j}from"./rig.COt8Za58.js";import"./unitcost.CRINWFT6.js";import"./kernel.DrC0sNW-.js";import"./topologycost.CuGW5prb.js";const x=720,V=268,a=46,J=122,p=x-a-J,ve=13,F=32,z=126,$=F+z,fe=$+13,ge=$+28,P=204,D=18,ye=P+D/2,xe=P+D+14,$e=256,g=500,w=4e6,l=o=>Math.log10(Math.max(1,o)),G=o=>Math.round(o).toLocaleString("en-US"),c=o=>`${o<0?"−":""}$${Math.abs(o)>=1e3?G(Math.abs(o)):Math.abs(o).toFixed(2)}`,B=o=>o>=1e6?`${(o/1e6).toFixed(1)} MB`:o>=1e5?`${Math.round(o/1e3)} KB`:o>=1e3?`${(o/1e3).toFixed(1)} KB`:`${Math.round(o)} B`,y=(o,s=0)=>`${(o*100).toFixed(s)}%`;class Me extends HTMLElement{connectedCallback(){const s=oe(this.getAttribute("mix")||"mixed"),e=ae(s);this.bytes=Math.round(e.bytesPerRequest),this.hit=e.byteHitRatio,this.fill=1,this.base=re("json-40k"),this.innerHTML=`
      <div class="panel">
        <canvas role="img" aria-label="The byte hit ratio a CDN must reach to beat origin egress, plotted against response size, with the reader's own hit ratio drawn across it, and a bar showing what a million requests costs to move split into the line called egress and the ones that are not."></canvas>
        <div class="readouts"></div>
        <p class="verdict"></p>
        <div class="controls">
          <label>
            response size
            <input type="range" data-s="bytes" min="0" max="100" step="1" value="${this.toLog(this.bytes)}"
                   aria-label="Bytes in one response, from 500 bytes to 4 megabytes">
            <output class="num" data-o="bytes"></output>
          </label>
          <label>
            byte hit ratio
            <input type="range" data-s="hit" min="0" max="100" step="1" value="${Math.round(this.hit*100)}"
                   aria-label="Share of delivered bytes the edge serves without fetching from the origin">
            <output class="num" data-o="hit"></output>
          </label>
          <label>
            origin→CDN price
            <input type="range" data-s="fill" min="0" max="100" step="5" value="100"
                   aria-label="Price of a cache fill as a share of internet egress; zero means the provider waives it">
            <output class="num" data-o="fill"></output>
          </label>
        </div>
      </div>`,this.canvas=this.querySelector("canvas");const v=this.querySelector(".readouts");this.ro={rps:S(v,"requests/s"),instances:S(v,"instances held"),compute:S(v,"compute/mo"),transfer:S(v,"transfer/mo"),be:S(v,"CDN pays above"),saving:S(v,"CDN saving/mo")},this.verdict=this.querySelector(".verdict");const r=u=>this.querySelector(`[data-s="${u}"]`);r("bytes").addEventListener("input",()=>{this.bytes=this.fromLog(Number(r("bytes").value)),this.draw()}),r("hit").addEventListener("input",()=>{this.hit=Number(r("hit").value)/100,this.draw()}),r("fill").addEventListener("input",()=>{this.fill=Number(r("fill").value)/100,this.draw()}),pe(()=>this.draw()),this.obs=new ResizeObserver(()=>this.draw()),this.obs.observe(this),this.draw()}disconnectedCallback(){this.obs?.disconnect()}toLog(s){return Math.round(100*(l(s)-l(g))/(l(w)-l(g)))}fromLog(s){return Math.round(Math.pow(10,l(g)+s/100*(l(w)-l(g))))}prices(){return{...h,originToCdnGB:this.fill*h.internetGB}}compare(s,e){return ne({classes:[{id:"x",requestShare:1,bytes:s,hitRatio:e}]},this.prices())}draw(){const s=ue(this),e=me(this.canvas,x,V);e.clearRect(0,0,x,V);const v={...this.base,responseBytes:this.bytes},r=le(v),u=he(),M=ce(this.prices()),d=K({bytesPerRequest:this.bytes},this.prices()),f=this.compare(this.bytes,this.hit),k=r.requestsPerMonth/1e6;this.querySelector('[data-o="bytes"]').textContent=B(this.bytes),this.querySelector('[data-o="hit"]').textContent=y(this.hit),this.querySelector('[data-o="fill"]').textContent=this.fill===0?"waived":`$${(this.fill*h.internetGB).toFixed(3)}/GB`;const T=t=>a+(l(t)-l(g))/(l(w)-l(g))*p,m=t=>$-Math.max(0,Math.min(1.06,t))*z,n=[];for(let t=0;t<=p;t++){const i=this.fromLog(t/p*100),b=K({bytesPerRequest:i},this.prices()),ie=b.breakEven===null?1.06:b.verdict==="always"?-.02:b.breakEven;n.push({x:a+t,b:i,be:ie})}const _=[];for(let t=0;t<n.length;){const i=this.hit>n[t].be;let b=t;for(;b+1<n.length&&this.hit>n[b+1].be===i;)b++;_.push({from:t,to:Math.min(b+1,n.length-1),positive:i}),t=b+1}e.globalAlpha=.26;for(const t of _){e.fillStyle=t.positive?s.teal:s.crimson,e.beginPath();for(let i=t.from;i<=t.to;i++)e.lineTo(n[i].x,m(Math.min(this.hit,n[i].be)));for(let i=t.to;i>=t.from;i--)e.lineTo(n[i].x,m(Math.max(this.hit,n[i].be)));e.closePath(),e.fill()}e.globalAlpha=1,e.strokeStyle=s.rule,e.lineWidth=1;for(const t of[0,.25,.5,.75,1]){const i=m(t);e.globalAlpha=t===0?1:.35,e.beginPath(),e.moveTo(a,i+.5),e.lineTo(a+p,i+.5),e.stroke(),C(e,`${t*100}%`,a-6,i,s.slate,{size:9,align:"right"})}e.globalAlpha=1;for(let t=Math.ceil(l(g));t<=l(w);t++){const i=Math.pow(10,t);if(i>w)break;C(e,B(i),T(i),fe,s.slate,{size:9,align:"center"})}e.save(),e.beginPath(),e.rect(a,F-8,p+1,z+9),e.clip(),e.strokeStyle=s.crimson,e.lineWidth=1.8,e.setLineDash([4,3]),e.beginPath(),n.forEach((t,i)=>i?e.lineTo(t.x,m(t.be)):e.moveTo(t.x,m(t.be))),e.stroke(),e.restore(),e.save(),e.strokeStyle=s.teal,e.lineWidth=1.8,e.beginPath(),e.moveTo(a,m(this.hit)),e.lineTo(a+p,m(this.hit)),e.stroke(),e.restore();const N=n[n.length-1].be,U=[{y:m(this.hit),text:`your bytes ${y(this.hit)}`,colour:s.teal},{y:m(Math.min(N,1.04)),text:`pays above ${y(Math.max(0,N),0)}`,colour:s.crimson}];e.save(),e.font="9px ui-monospace, SFMono-Regular, Menlo, monospace";const Q=Math.max(...U.map(t=>e.measureText(t.text.toUpperCase()).width));e.restore(),Q<=J-8&&be(e,U,{x:a+p+6,align:"left",minGap:12,minY:F-4,maxY:$});const O=Math.round(T(this.bytes))+.5;e.save(),e.strokeStyle=s.amber,e.lineWidth=1.5,e.beginPath(),e.moveTo(O,F-6),e.lineTo(O,$+4),e.stroke(),e.restore();const Y=Number.isFinite(M)&&M>g&&M<w;if(Y){const t=Math.round(T(M))+.5;e.strokeStyle=s.slate,e.lineWidth=1,e.beginPath(),e.moveTo(t,$-10),e.lineTo(t,$+4),e.stroke()}j(e,[{x:T(this.bytes),text:"yours",colour:s.amber},...Y?[{x:T(M),text:`no size pays below ${B(M)}`,colour:s.slate}]:[]],{y:ge,minGap:8,size:9,minX:a,maxX:x-4});const W=this.bytes/1e9,q=[{id:"internet",text:"internet egress",v:W*1e6*h.internetGB,colour:s.amber},{id:"other",text:"other crossings",v:W*1e6*(u.readCostPerGB-h.internetGB),colour:s.crimson},{id:"replication",text:"replication",v:this.base.writeBytesPerRequest/1e9*1e6*u.writeCostPerGB,colour:s.slate}],H=q.reduce((t,i)=>t+i.v,0);let R=a;for(const t of q){const i=t.v/H*p;e.fillStyle=t.colour,e.globalAlpha=.85,e.fillRect(R,P,Math.max(.5,i),D),t.centre=R+i/2,t.wid=i,R+=i}e.globalAlpha=1,e.strokeStyle=s.rule,e.lineWidth=1,e.strokeRect(a+.5,P+.5,p-1,D-1),C(e,"per 1M",a-6,ye,s["ink-soft"],{size:9,align:"right"});const Z=1e6*h.instanceHour/(this.base.rpsPerInstance*3600*this.base.utilisation),A=Math.round(a+Math.min(1,Z/H)*p)+.5;e.strokeStyle=s.teal,e.lineWidth=1.5,e.beginPath(),e.moveTo(A,P-5),e.lineTo(A,P+D+5),e.stroke(),j(e,[...q.map(t=>({x:t.centre,text:t.text,colour:t.colour})),{x:A,text:"all compute",colour:s.teal}],{y:xe,minGap:8,size:9,minX:a,maxX:x-4});const X=`${G(this.base.rps)} rps · 3 zones · rf 3 · 2 regions · $${h.internetGB}/GB out · $${h.cdnEgressGB}/GB cdn · $${h.cdnPer10kRequests}/10k req · prices are illustrative`;e.save(),e.font="9px ui-monospace, SFMono-Regular, Menlo, monospace";const ee=e.measureText(X).width<=x-a-4,I=`one delivered byte crosses ${u.readCrossings.toFixed(2)} priced boundaries — $${u.readCostPerGB.toFixed(4)}/GB, ${u.multiplierVsEgressLine.toFixed(2)}× the egress line`,te=e.measureText(I).width<=x-a-4;e.restore(),C(e,ee?X:`${G(this.base.rps)} rps · 3 zones · rf 3 · 2 regions · illustrative prices`,a,ve,s["ink-soft"],{size:9,caps:!1}),C(e,te?I:`${u.readCrossings.toFixed(2)} priced crossings · ${u.multiplierVsEgressLine.toFixed(2)}× the egress line`,a,$e,s["ink-soft"],{size:9,caps:!1});const L=d.verdict==="never"?"no hit ratio":d.verdict==="always"?"any hit ratio":y(d.breakEven,1),E=d.verdict==="always"||d.verdict==="threshold"&&this.hit>d.breakEven;this.ro.rps.set(G(this.base.rps)),this.ro.instances.set(r.instances.toFixed(2)),this.ro.compute.set(`${c(r.computeUSD)}`),this.ro.transfer.set(`${c(r.transferUSD)}`,r.transferOverCompute>20?"bad":"warn"),this.ro.be.set(L,d.verdict==="never"?"bad":E?"ok":"warn"),this.ro.saving.set(c(f.savingPerMillionUSD*k),E?"ok":"bad");const se=d.verdict==="never"?`At ${B(this.bytes)} the CDN's $${Se()} per 10,000 requests works out to $${f.breakEven.feePerGB.toFixed(3)} per GB delivered — more than the entire $${(h.internetGB-h.cdnEgressGB).toFixed(2)} per-GB saving — so no hit ratio pays, and this one costs ${c(-f.savingPerMillionUSD*k)} a month.`:d.verdict==="always"?`With the fill ${this.fill===0?"waived":`at $${(this.fill*h.internetGB).toFixed(3)}/GB`} the hit ratio has left the expression: the CDN pays at any hit ratio, including zero, and here it saves ${c(f.savingPerMillionUSD*k)} a month.`:`A CDN has to clear ${y(d.breakEven,1)} of BYTES — not requests — to beat that. At ${y(this.hit)} it ${E?"saves":"costs"} ${c(Math.abs(f.savingPerMillionUSD)*k)} a month, ${y(Math.abs(f.savingShare),1)} ${f.savingPerMillionUSD>0?"off":"on top of"} the origin's egress.`;this.verdict.textContent=`${B(this.bytes)} responses at ${G(this.base.rps)} rps: ${c(r.transferUSD)} a month to move the bytes, ${r.transferOverCompute.toFixed(r.transferOverCompute<10?1:0)}× the ${c(r.computeUSD)} of compute — and the request rate, the fleet size and the compute bill are the same at every position of this slider. ${se}`,this.canvas.setAttribute("aria-label",`At ${B(this.bytes)} responses the transfer bill is ${c(r.transferUSD)} a month against ${c(r.computeUSD)} of compute, and the fleet is ${r.instances.toFixed(2)} instances at every response size. A CDN needs ${L==="no hit ratio"?"a hit ratio no cache can reach":L} of bytes to pay for itself; at ${y(this.hit)} it ${E?"saves":"costs"} ${c(Math.abs(f.savingPerMillionUSD)*k)} a month.`)}}const Se=()=>de.find(o=>o.id==="cdnPer10kRequests").value;customElements.define("egress-rig",Me);
