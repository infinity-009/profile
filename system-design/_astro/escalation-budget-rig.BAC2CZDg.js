import{e as U,a as J,J as X,j as G,b as V}from"./model-routing.DxmR-34v.js";import{readout as y}from"./rig.CyiChFZe.js";import{a as I}from"./inference-gateway.B5Ydqc25.js";import{M as C}from"./reasoning-effort.BYq25RAu.js";import"./kernel.DrC0sNW-.js";import"./limiters.CHKI6pwP.js";import"./bulkhead.DGoNKOeA.js";import"./inference.zvSQdG3n.js";import"./accelerators.BdwR2ykV.js";import"./ttft-perception.0rancOKD.js";const T=720,Y=128,K=30,r=250,j=66,Q=44,q=5,Z=[{recall:.5,label:"half the errors"},{recall:.8,label:"4 in 5"},{recall:.95,label:"19 in 20"}],tt="http://www.w3.org/2000/svg",o=(s,t={},l)=>{const n=document.createElementNS(tt,s);for(const[x,e]of Object.entries(t))n.setAttribute(x,String(e));return l!==void 0&&(n.textContent=l),n},v=s=>`${Math.round(s*100)}%`,h=s=>`${(s*100).toFixed(1)}%`,et=s=>s>=100?`$${Math.round(s)}`:`$${s.toFixed(1)}`,P=s=>s>=1e3?`${s%1e3===0?s/1e3:(s/1e3).toFixed(1)}k`:String(Math.round(s)),A={logprob:"logprobs","self-critique":"self-critique","judge-model":"strong judge"};class at extends HTMLElement{connectedCallback(){this.mixIdx=0,this.promptIdx=4,this.prompts=[2e3,8e3,32e3,96e3,I.promptMedian,6e5],this.innerHTML=`
      <div class="panel">
        <svg viewBox="0 0 ${T} 300" role="img" width="100%"
             aria-label="An escalation-rate axis with the break-even marked, and one row per confidence signal showing the escalation rate each produces at three named error-catching targets."></svg>
        <div class="readouts"></div>
        <p class="verdict"></p>
        <div class="controls">
          <label>
            workload
            <select data-s="mix" aria-label="Which mix of task classes the workload is">
              ${Object.keys(C).map((l,n)=>`<option value="${n}"${n===0?" selected":""}>${C[l].name}</option>`).join("")}
            </select>
          </label>
          <label>
            prompt size
            <input type="range" min="0" max="5" step="1" value="4" data-s="prompt"
                   aria-label="How large the prompt being re-sent to the second model is">
            <output class="num" data-o="prompt"></output>
          </label>
        </div>
      </div>`,this.svg=this.querySelector("svg");const t=this.querySelector(".readouts");this.ro={budget:y(t,"escalation budget, priced honestly"),warm:y(t,"and what the usual arithmetic says"),esc:y(t,"cost of one escalated call"),strong:y(t,"cost of one always-strong call"),inside:y(t,"operating points inside the budget")},this.verdict=this.querySelector(".verdict"),this.querySelector('[data-s="mix"]').addEventListener("change",l=>{this.mixIdx=Number(l.target.value),this.draw()}),this.querySelector('[data-s="prompt"]').addEventListener("input",l=>{this.promptIdx=Number(l.target.value),this.draw()}),this.draw()}draw(){const t=this.svg;for(;t.firstChild;)t.removeChild(t.firstChild);const l=C[Object.keys(C)[this.mixIdx]].shares,n=this.prompts[this.promptIdx],x={promptTokens:n},e=U({...x}),L=J({tierId:"strong",shares:l,...x}),m=Y,g=T-K,u=a=>m+Math.max(0,Math.min(1,a))*(g-m),d=u(Math.max(0,e.cold));t.appendChild(o("rect",{x:m,y:r-7,width:Math.max(0,d-m),height:14,fill:"var(--teal)",opacity:.8})),t.appendChild(o("rect",{x:d,y:r-7,width:Math.max(0,g-d),height:14,fill:"var(--crimson)",opacity:.7}));for(const a of[0,.2,.4,.6,.8,1])t.appendChild(o("line",{x1:u(a),y1:r+8,x2:u(a),y2:r+13,stroke:"var(--rule)"})),t.appendChild(o("text",{x:u(a),y:r+26,fill:"var(--slate)","font-size":9,"text-anchor":"middle"},v(a)));t.appendChild(o("text",{x:m,y:r+44,fill:"var(--ink-soft)","font-size":9},"share of requests escalated to the strong model"));const M=u(Math.max(0,e.warm)),F=M>d&&M-d<210||d>g-150;t.appendChild(o("line",{x1:d,y1:r-16,x2:d,y2:r+7,stroke:"var(--ink)","stroke-width":2})),t.appendChild(o("text",{x:F?d-6:d+6,y:r-22,fill:"var(--ink)","font-size":10,"text-anchor":F?"end":"start"},`${h(e.cold)} — the budget`));const $=M;Math.abs($-d)>2&&(t.appendChild(o("line",{x1:$,y1:r-34,x2:$,y2:r+20,stroke:"var(--slate)","stroke-width":1.5,"stroke-dasharray":"4 3"})),t.appendChild(o("text",{x:$-6,y:r-38,fill:"var(--slate)","font-size":9,"text-anchor":"end"},`${h(e.warm)} if the prefix stayed warm`))),t.appendChild(o("text",{x:8,y:j-24,fill:"var(--ink-soft)","font-size":9},"what each confidence signal escalates, to catch:"));let c=0,f=0;const k=X.map((a,w)=>{const S=G({shares:l,judgeId:a.id,...x}).points,p=j+w*Q;t.appendChild(o("text",{x:8,y:p+4,fill:"var(--ink)","font-size":10},A[a.id]??a.id)),t.appendChild(o("text",{x:8,y:p+17,fill:"var(--slate)","font-size":9},`AUC ${V({dPrime:a.dPrime,threshold:0}).auc.toFixed(2)}`)),t.appendChild(o("line",{x1:m,y1:p,x2:g,y2:p,stroke:"var(--rule)"}));const O=Z.map(i=>{const b=S.reduce((E,_)=>Math.abs(_.recall-i.recall)<Math.abs(E.recall-i.recall)?_:E,S[0]);f++;const z=b.escalationRate<=e.cold;return z&&c++,{...i,pt:b,ok:z,judge:a,x:u(b.escalationRate)}});for(const i of O)t.appendChild(o("circle",{cx:i.x,cy:p,r:q,fill:i.ok?"var(--teal)":"var(--crimson)",opacity:.9})),t.appendChild(o("line",{x1:i.x,y1:p+q,x2:i.x,y2:r-9,stroke:i.ok?"var(--teal)":"var(--crimson)",opacity:.28}));return O.forEach((i,b)=>{t.appendChild(o("text",{x:Math.min(i.x,g-2),y:p-9-b%2*11,fill:i.ok?"var(--teal)":"var(--crimson)","font-size":9,"text-anchor":i.x>g-60?"end":"middle"},`${i.label} → ${v(i.pt.escalationRate)}`))}),{judge:a,marks:O}});this.ro.budget.set(h(e.cold),e.cold>.3?"ok":e.cold>.15?"warn":"bad"),this.ro.warm.set(`${h(e.warm)} — ${e.overstatement.toFixed(2)}× out`,e.overstatement>3?"bad":"warn"),this.ro.esc.set(`$${e.escalatedCold.toFixed(4)}`,"bad"),this.ro.strong.set(`$${e.strongWarm.toFixed(4)}`,"warn"),this.ro.inside.set(`${c} of ${f}`,c===0?"bad":c===f?"ok":"warn");const W=k.flatMap(a=>a.marks).filter(a=>!a.ok).sort((a,w)=>w.pt.escalationRate-a.pt.escalationRate)[0],R=k.flatMap(a=>a.marks).filter(a=>a.ok).sort((a,w)=>w.pt.recall-a.pt.recall)[0],B=`A cascade on a ${P(n)}-token prompt can escalate ${h(e.cold)} of requests before it costs more than sending everything to the strong model. One escalated call is $${e.escalatedCold.toFixed(4)} against $${e.strongWarm.toFixed(4)} for a plain strong-model call — ${(e.escalatedCold/e.strongWarm).toFixed(1)}× — because escalating is a model switch and a model switch drops the prefix-cache hit rate to 8%. `,D=e.overstatement>1.2?`Charge that call as though its prefix were still warm, which is how a cascade is usually costed, and the budget reads ${h(e.warm)}: out by ${e.overstatement.toFixed(2)}×. `:`At this prompt size the two prices are nearly the same, so the usual arithmetic is close to right: ${h(e.warm)} against ${h(e.cold)}. The penalty is proportional to the prompt being re-written, and there is not much of one here. `,H=`Sending everything to the strong model costs ${et(L.usdPer1000)} per thousand turns on this workload. `,N=c===0?`And not one of the nine operating points fits. The cheapest thing any of these signals can do — catch half the weak model's errors with ${A[k[0].judge.id]} — already escalates ${v(k[0].marks[0].pt.escalationRate)}, and the most cautious sensible setting on the panel is ${v(W.pt.escalationRate)}. Every crimson dot is a cascade that costs more than the model it was avoiding.`:c===f?"At this prompt size every operating point on the panel fits inside the budget, which is the regime cascades were designed for — small prompts, cheap re-sends. Drag the prompt size up and watch the teal band shrink under the dots.":`${c} of the ${f} fit. The most errors any of them catches while staying inside the budget is ${v(R.pt.recall)}, with ${A[R.judge.id]} — so the affordable cascade is the one that escalates too rarely to change much, and the useful one is over budget. That is the trade, stated as sharply as the numbers allow.`;this.verdict.textContent=B+D+H+N,t.setAttribute("viewBox",`0 0 ${T} ${r+58}`),this.svg.setAttribute("aria-label",`An escalation-rate axis. On a ${Math.round(n)}-token prompt the cascade's escalation budget is ${(e.cold*100).toFixed(1)} percent, against ${(e.warm*100).toFixed(1)} percent if the escalated call's prefix were still warm. Of nine named operating points across three confidence signals, ${c} fall inside the budget.`),this.querySelector('[data-o="prompt"]').textContent=`${P(n)} tokens${n===I.promptMedian?" — the measured median agentic turn":""}`}}customElements.get("escalation-budget-rig")||customElements.define("escalation-budget-rig",at);
