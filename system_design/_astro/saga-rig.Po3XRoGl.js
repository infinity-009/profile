import{readout as F,onThemeChange as Q,palette as Z,fitCanvas as tt,label as g,spreadLabels as et}from"./rig.COt8Za58.js";const N=o=>3*o,E=o=>o*(o-1)/2;function I(o,e){const t=Array.isArray(o)?o.slice(0,e):Array(e).fill(o);for(;t.length<e;)t.push(t[t.length-1]??1);return t}function st(o,e){const t=I(o,e),a=[1];for(let s=1;s<=e;s++)a.push(a[s-1]*t[s-1]);return a}function at({n:o,p:e,q:t}){const a=1-e,s=st(t,o),n=[];for(let r=1;r<=o;r++){const h=e**(r-1)*a;n.push({step:r,undos:r-1,failedHere:h,compensated:h*s[r-1],stuck:h*(1-s[r-1])})}return n}function B({n:o,p:e,q:t}){const a=at({n:o,p:e,q:t}),s=e**o;let n=0,r=0;for(const h of a)n+=h.compensated,r+=h.stuck;return{completed:s,compensated:n,stuck:r,stuckShareOfFailures:s>=1?0:r/(1-s),states:N(o)}}function ot({n:o,p:e,q:t,visibleStep:a=1,stepMs:s=250}){const n=a,r=1-e,h=I(t,o),R=e**n;let v=0,c=0,w=0;if(o-n>0){const d=R*e**(o-n);v+=d*(o-n)*s,c+=d}for(let d=n+1;d<=o;d++){const k=R*e**(d-n-1)*r;let u=1;for(let S=n;S<=d-1;S++)u*=h[S-1];v+=k*u*2*(d-n)*s,c+=k*u,w+=k*(1-u)}return{meanMs:v,exposedRate:c,permanentRate:w,meanGivenExposedMs:c>0?v/c:0}}const nt=({n:o,stepMs:e=250})=>e*E(o),M=720,D=268,p=104,it=104,H=M-p-it,X=13,O=32,Y=132,_=O+Y,rt=198,lt=212,z=16,ct=26,ht=10,A=250,U=ht*A,$=o=>p+Math.min(o,U)/U*H,V=180,G=V*86400;class dt extends HTMLElement{connectedCallback(){this.n=4,this.visibleStep=1,this.f=.001,this.g=.01,this.innerHTML=`
      <div class="panel">
        <canvas role="img" aria-label="A staircase of shaded bands, one per invariant a saga breaks, whose total area is the saga's exposure; below it two bars comparing how many sagas per day end in a state no retry can clear."></canvas>
        <div class="readouts"></div>
        <p class="verdict"></p>
        <div class="controls">
          <label>
            steps
            <input type="range" data-s="n" min="2" max="10" step="1" value="4"
                   aria-label="How many local transactions the saga is made of">
            <output class="num" data-o="n"></output>
          </label>
          <label>
            the visible write is step
            <input type="range" data-s="j" min="1" max="4" step="1" value="1"
                   aria-label="Which step commits the invariant a customer can see">
            <output class="num" data-o="j"></output>
          </label>
          <label>
            step failure rate
            <input type="range" data-s="f" min="1" max="50" step="1" value="10"
                   aria-label="How often one local transaction fails, in tenths of a percent">
            <output class="num" data-o="f"></output>
          </label>
          <label>
            undo failure rate
            <input type="range" data-s="g" min="1" max="100" step="1" value="10"
                   aria-label="How often a compensating transaction fails, in tenths of a percent">
            <output class="num" data-o="g"></output>
          </label>
        </div>
      </div>`,this.canvas=this.querySelector("canvas");const e=this.querySelector(".readouts");this.ro={window:F(e,"visible for"),readers:F(e,"readers in it"),partial:F(e,"partial now"),stuck:F(e,"stuck per day"),states:F(e,"saga states")},this.verdict=this.querySelector(".verdict");const t=s=>this.querySelector(`[data-s="${s}"]`),a=(s,n)=>{t(s).addEventListener("input",()=>{n(Number(t(s).value)),this.labels()}),t(s).addEventListener("change",()=>{n(Number(t(s).value)),this.draw()})};a("n",s=>{this.n=s;const n=t("j");n.max=String(s),this.visibleStep>s&&(this.visibleStep=s,n.value=String(s))}),a("j",s=>this.visibleStep=s),a("f",s=>this.f=s/1e3),a("g",s=>this.g=s/1e3),Q(()=>this.draw()),this.obs=new ResizeObserver(()=>this.draw()),this.obs.observe(this),this.draw()}disconnectedCallback(){this.obs?.disconnect()}labels(){this.querySelector('[data-o="n"]').textContent=String(this.n),this.querySelector('[data-o="j"]').textContent=`${this.visibleStep} of ${this.n}`,this.querySelector('[data-o="f"]').textContent=`${(this.f*100).toFixed(1)}%`,this.querySelector('[data-o="g"]').textContent=`${(this.g*100).toFixed(1)}%`}draw(){const e=Z(this),t=tt(this.canvas,M,D);t.clearRect(0,0,M,D),this.labels();const a=this.n,s=Math.min(this.visibleStep,a),n=1-this.f,r=1-this.g,h=B({n:a,p:n,q:r}),R=Array.from({length:a},(i,l)=>l===a-2?0:r),v=B({n:a,p:n,q:R}),c=ot({n:a,p:n,q:r,visibleStep:s,stepMs:A}),w=nt({n:a,stepMs:A});g(t,"each invariant the saga breaks, and how long it stays broken  ·  time →",p-96,X,e["ink-soft"],{size:9,caps:!1}),g(t,`total ${(w/1e3).toFixed(2)}s of broken invariant = 250ms × ${E(a)} pair${E(a)===1?"":"s"}`,M-4,X,e["ink-soft"],{size:9,align:"right",caps:!1});const d=a*A;t.strokeStyle=e.rule,t.lineWidth=1,t.beginPath(),t.moveTo($(d),O),t.lineTo($(d),_),t.stroke();const k=Y/a,u=Math.min(18,Math.max(6,k-3));for(let i=1;i<=a;i++){const l=O+(i-.5)*k-u/2,b=$(i*A),m=$(a*A),x=i===s,C=x?e.amber:e.slate;if(g(t,`step ${i}`,p-8,l+u/2,x?e.amber:e["ink-soft"],{size:9,align:"right",caps:!1}),m-b<1){t.strokeStyle=C,t.lineWidth=x?2:1,t.beginPath(),t.moveTo(m,l),t.lineTo(m,l+u),t.stroke(),g(t,"never visible",m+6,l+u/2,C,{size:9,caps:!1});continue}t.fillStyle=C,t.globalAlpha=x?.55:.22,t.fillRect(b,l,m-b,u),t.globalAlpha=1,x&&(t.strokeStyle=e.amber,t.lineWidth=1,t.strokeRect(b+.5,l+.5,m-b-1,u-1),g(t,`${(c.meanMs/1e3).toFixed(2)}s`,m+6,l+u/2,e.amber,{size:9,caps:!1}))}t.strokeStyle=e.rule,t.lineWidth=1,t.beginPath(),t.moveTo(p,_+.5),t.lineTo(p+H,_+.5),t.stroke();const S=[];let j=!1;for(const i of[0,500,1e3,1500,2e3,2500]){if(i>U)continue;const l=Math.abs(i-d)<60;l&&(j=!0),S.push({x:$(i),text:l?`${i/1e3}s · saga ends`:i===0?"0":`${i/1e3}s`,colour:l?e["ink-soft"]:e.slate})}j||S.push({x:$(d),text:"saga ends",colour:e["ink-soft"]}),et(t,S,{y:_+12,minGap:7,size:8,minX:p-6,maxX:M-6}),g(t,"sagas per day that no retry can resolve, at 180 sagas/s",p-96,rt,e["ink-soft"],{size:9,caps:!1});const f=h.stuck*G,T=v.stuck*G,J=Math.max(f,T,1)*1.02,q=i=>i>=.01?`${(i*100).toFixed(1)}%`:`${+(i*100).toPrecision(2)}%`;[{v:f,colour:e.teal,text:`${Math.round(f).toLocaleString("en-US")} — every undo merely unreliable (${q(h.stuckShareOfFailures)} of failures)`},{v:T,colour:e.crimson,text:`${Math.round(T).toLocaleString("en-US")} — one undo impossible, at the least damaging step (${q(v.stuckShareOfFailures)})`}].forEach((i,l)=>{const b=lt+l*ct,m=Math.max(2,i.v/J*H);t.fillStyle=i.colour,t.globalAlpha=.7,t.fillRect(p,b,m,z),t.globalAlpha=1,t.save(),t.font="9px ui-monospace, SFMono-Regular, Menlo, monospace";const x=t.measureText(i.text).width;t.restore(),p+m+6+x<=M-4?g(t,i.text,p+m+6,b+z/2,e["ink-soft"],{size:9,caps:!1}):g(t,i.text,p+8,b+z/2,e.paper,{size:9,caps:!1})});const L=3*c.meanMs/1e3,P=V*c.meanMs/1e3;this.ro.window.set(`${(c.meanMs/1e3).toFixed(2)}s`,c.meanMs>500?"bad":c.meanMs>0?"warn":"ok"),this.ro.readers.set(L.toFixed(2),L>1?"bad":L>.2?"warn":"ok"),this.ro.partial.set(Math.round(P).toLocaleString("en-US"),P>50?"bad":P>5?"warn":"ok"),this.ro.stuck.set(Math.round(f).toLocaleString("en-US"),f>500?"bad":f>50?"warn":"ok"),this.ro.states.set(String(N(a)),a>6?"bad":"warn");const W=(h.completed*100).toFixed(1),y=f>0?T/f:0,K=!Number.isFinite(y)||y<=0?"":y>=5?` One impossible compensation multiplies the unresolvable rate by ${y>=100?Math.round(y).toLocaleString("en-US"):y.toFixed(1)}×, and making the other undos more reliable barely moves it.`:` Here the ordinary undos already fail often enough that one impossible step adds only ${y.toFixed(1)}× on top — at these rates undo reliability is the problem and the irreversible step is not.`;this.verdict.textContent=(h.completed>=.99?`${W}% of sagas complete on the first pass, and that is not the cost. The cost is that step `:`Only ${W}% of sagas complete on the first pass, so here the completion rate is a cost too — but it is not the one this instrument is about. That one is that step `)+`${s}'s invariant is false for ${(c.meanMs/1e3).toFixed(2)}s, which at 180 sagas/s means ${Math.round(P).toLocaleString("en-US")} entities are in a visibly partial state at any instant, and that ${Math.round(f).toLocaleString("en-US")} sagas a day end somewhere a retry cannot reach. `+(s===a?`You have put the visible write last, so its own row is empty — but the shaded total is still ${(w/1e3).toFixed(2)}s. The exposure moved to step 1, it did not go away.`:`Drag the visible write to step ${a} and watch its row empty while the triangle stays exactly the same size: reordering chooses which invariant gets the long window, and cannot shrink the total.`)+K,this.canvas.setAttribute("aria-label",`A ${a}-step saga at ${(this.f*100).toFixed(1)} percent step failure and ${(this.g*100).toFixed(1)} percent undo failure. Step ${s}'s invariant is observably false for ${(c.meanMs/1e3).toFixed(2)} seconds, the total across all ${a} invariants is ${(w/1e3).toFixed(2)} seconds, and ${Math.round(f).toLocaleString("en-US")} sagas a day end in a state needing a human — ${Math.round(T).toLocaleString("en-US")} if one compensation is impossible.`)}}customElements.define("saga-rig",dt);
