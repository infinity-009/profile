import{S as w,i as _,e as D,a as H,c as E}from"./coupling.CPdsKbd2.js";import{readout as m,onThemeChange as O,palette as B,fitCanvas as N,label as h}from"./rig.COt8Za58.js";const p=720,F=322,a=52,X=128,l=p-a-X,Y=13,q=34,M=150,T=q+M,P=232,S=15,I=24,y=1,L=200,u=g=>Math.log10(Math.max(1e-9,g)),C=g=>a+(u(g)-u(y))/(u(L)-u(y))*l;class K extends HTMLElement{connectedCallback(){this.services=12,this.functions=4,this.alignment=0,this.innerHTML=`
      <div class="panel">
        <canvas role="img" aria-label="A curve of how many services must deploy together against how many services exist, with a ceiling line at the number of functions a change touches."></canvas>
        <div class="readouts"></div>
        <p class="verdict"></p>
        <div class="controls">
          ${w.map(t=>`<button data-sh="${t.id}" aria-pressed="false" title="${t.note}">${t.label}</button>`).join("")}
        </div>
        <div class="controls">
          <label>
            services
            <input type="range" data-s="k" min="1" max="200" step="1" value="12"
                   aria-label="How many services the system is split into">
            <output class="num" data-o="k"></output>
          </label>
          <label>
            functions a change touches
            <input type="range" data-s="f" min="1" max="12" step="1" value="4"
                   aria-label="How many distinct pieces of behaviour an average change edits">
            <output class="num" data-o="f"></output>
          </label>
          <label>
            boundary alignment
            <input type="range" data-s="a" min="0" max="100" step="1" value="0"
                   aria-label="What fraction of edits land on the service that owns the capability">
            <output class="num" data-o="a"></output>
          </label>
        </div>
      </div>`,this.canvas=this.querySelector("canvas");const i=this.querySelector(".readouts");this.ro={unit:m(i,"deploy unit"),got:m(i,"independence got"),paid:m(i,"services paid for"),agree:m(i,"agreements per release")},this.verdict=this.querySelector(".verdict");for(const t of this.querySelectorAll("[data-sh]"))t.addEventListener("click",()=>{const o=w.find(f=>f.id===t.dataset.sh);this.services=o.services,this.functions=o.functions,this.alignment=o.alignment,this.sync(),this.draw()});const e=t=>this.querySelector(`[data-s="${t}"]`),n=(t,o)=>{e(t).addEventListener("input",()=>{o(Number(e(t).value)),this.labels()}),e(t).addEventListener("change",()=>{o(Number(e(t).value)),this.draw()})};n("k",t=>this.services=t),n("f",t=>this.functions=t),n("a",t=>this.alignment=t/100),O(()=>this.draw()),this.obs=new ResizeObserver(()=>this.draw()),this.obs.observe(this),this.draw()}disconnectedCallback(){this.obs?.disconnect()}sync(){this.querySelector('[data-s="k"]').value=String(this.services),this.querySelector('[data-s="f"]').value=String(this.functions),this.querySelector('[data-s="a"]').value=String(Math.round(this.alignment*100)),this.labels()}labels(){this.querySelector('[data-o="k"]').textContent=String(this.services),this.querySelector('[data-o="f"]').textContent=String(this.functions),this.querySelector('[data-o="a"]').textContent=`${Math.round(this.alignment*100)}%`}draw(){const i=B(this),e=N(this.canvas,p,F);e.clearRect(0,0,p,F),this.labels();const n=_({services:this.services,functions:this.functions,alignment:this.alignment}),t=Math.max(1.3,this.functions*1.22),o=s=>T-Math.min(s,t)/t*M;for(const s of this.querySelectorAll("[data-sh]")){const d=w.find(c=>c.id===s.dataset.sh),r=d.services===this.services&&d.functions===this.functions&&Math.abs(d.alignment-this.alignment)<.005;s.setAttribute("aria-pressed",String(r)),s.style.borderColor=r?"var(--amber)":"",s.style.color=r?"var(--amber)":""}h(e,"services that must deploy together  ↑     ·     how many services exist  →",a-44,Y,i["ink-soft"],{size:9,caps:!1});const f=o(this.functions);e.strokeStyle=i.crimson,e.setLineDash([4,4]),e.lineWidth=1,e.beginPath(),e.moveTo(a,f),e.lineTo(a+l,f),e.stroke(),e.setLineDash([]);const b=`ceiling — the ${this.functions} functions the change touches`;e.save(),e.font="9px ui-monospace, SFMono-Regular, Menlo, monospace";const z=e.measureText(b.toUpperCase()).width;e.restore(),a+l+6+z<=p-4?h(e,b,a+l+6,f,i.crimson,{size:9,caps:!1}):h(e,b,a+l-6,f-7,i.crimson,{size:9,align:"right",caps:!1});for(const[s,d,r]of[[0,i.crimson,!0],[this.alignment,i.teal,!1]]){e.strokeStyle=d,e.lineWidth=s===this.alignment?2:1,r&&this.alignment>.005&&e.setLineDash([2,3]),e.beginPath();for(let c=0;c<=l;c+=2){const v=Math.pow(10,u(y)+c/l*(u(L)-u(y))),W=D({services:Math.max(1,v),functions:this.functions,alignment:s}),A=o(W);c===0?e.moveTo(a+c,A):e.lineTo(a+c,A)}if(e.stroke(),e.setLineDash([]),r&&this.alignment<=.005)break}e.strokeStyle=i.rule,e.lineWidth=1,e.strokeRect(a+.5,q+.5,l,M);for(const s of[1,3,12,50,200])h(e,String(s),C(s),T+12,i.slate,{size:9,caps:!1});for(const s of[1,2,3,4,6,8,12])s>t||h(e,String(s),a-6,o(s),i.slate,{size:9,align:"right",caps:!1});const R=C(this.services),U=o(n.deployUnit);e.strokeStyle=i.amber,e.lineWidth=2,e.beginPath(),e.arc(R,U,5.5,0,Math.PI*2),e.stroke(),h(e,"what you paid, and what arrived",a,P-12,i["ink-soft"],{size:9,caps:!1});const $=l;[{text:`${this.services} services' worth of independent deploys — what you are paying for`,frac:1,colour:i.crimson},{text:`${n.effectiveStreams.toFixed(1)} independent streams — what arrived, ${(n.realisedFraction*100).toFixed(0)}%`,frac:n.realisedFraction,colour:i.teal}].forEach((s,d)=>{const r=P+d*I;e.fillStyle=s.colour,e.globalAlpha=.7,e.fillRect(a,r,Math.max(2,s.frac*$),S),e.globalAlpha=1,e.save(),e.font="9px ui-monospace, SFMono-Regular, Menlo, monospace";const c=e.measureText(s.text.toUpperCase()).width;e.restore();const v=a+Math.max(2,s.frac*$)+6;v+c<=p-4?h(e,s.text,v,r+S/2,i["ink-soft"],{size:9,caps:!1}):h(e,s.text,a+8,r+S/2,i.paper,{size:9,caps:!1})});const x=E(Math.round(n.deployUnit));this.ro.unit.set(n.deployUnit.toFixed(2),n.deployUnit>2?"bad":n.deployUnit>1.3?"warn":"ok"),this.ro.got.set(`${(n.realisedFraction*100).toFixed(0)}%`,n.realisedFraction>.8?"ok":n.realisedFraction<.4?"bad":"warn"),this.ro.paid.set(String(this.services),this.services>30?"bad":"warn"),this.ro.agree.set(String(x),x>3?"bad":x>0?"warn":"ok");const k=H({services:this.services,functions:this.functions});this.verdict.textContent=`An average change touches ${n.deployUnit.toFixed(2)} of your ${this.services} services, so that is your deploy unit — you collected ${(n.realisedFraction*100).toFixed(0)}% of the independence ${this.services} services could have given, and you are paying for all ${this.services}. `+(this.alignment<.05?`Drag services to 200 and watch the curve climb rather than fall: with misaligned boundaries the deploy unit rises toward ${this.functions}, the size of the change itself. Splitting harder is not the fix — the boundary alignment slider is.`:`At zero alignment the same system would deploy ${k.toFixed(2)} services per change. The ${Math.round(this.alignment*100)}% alignment you have is worth ${(k/n.deployUnit).toFixed(1)}× on that, and it cost nothing at runtime — only a different choice about where the boundaries go.`),this.canvas.setAttribute("aria-label",`With ${this.services} services, a change touching ${this.functions} functions, and ${Math.round(this.alignment*100)} percent boundary alignment, the deploy unit is ${n.deployUnit.toFixed(2)} services and the independence realised is ${Math.round(n.realisedFraction*100)} percent.`)}}customElements.define("coupling-rig",K);
